#include<stdio.h>
int main(){
	int n,arr[3];scanf("%d %d %d %d",&n,&arr[0],&arr[1],&arr[2]);
	int min=arr[0],max=arr[0];
	int keys[3];
	for(int i=1;i<3;i++){
		if(arr[i]<min) min=arr[i];
		if(arr)
	}
}